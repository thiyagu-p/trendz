<%= form_for(@watchlist) do |f| %>
    <% if @watchlist.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@watchlist.errors.count, "error") %> prohibited this watchlist from being saved:</h2>

          <ul>
            <% @watchlist.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>
    <div>
      <%= f.label :stocks %>
      <%= f.select :stock_ids, Stock.all.map {|stock| [stock.symbol, stock.id]}, {}, { :multiple => true, :size => 10 } %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
